let list=["delhi","calcutta","pune","mumbai"];
console.log(list.length,list);

//remove an element :: from end
list.pop();
console.log(list.length,list);

//remove the element :: from start
list.shift();
console.log(list.length,list);

//Assignment is remove the element the element from in between
//splice method